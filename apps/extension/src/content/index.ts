// Bootstrap do content script. Substituído na Tarefa 11 pelo pipeline real
// (observers, video-registry, overlay). Por enquanto não faz nada perceptível.
function bootstrap(): void {
  // no-op
}

bootstrap();
